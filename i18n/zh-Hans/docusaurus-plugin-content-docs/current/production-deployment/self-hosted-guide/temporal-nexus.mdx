---
id: nexus
title: 自托管 Temporal Nexus
sidebar_label: Temporal Nexus
description: 在自托管 Temporal 服务中使用 Nexus。
toc_max_heading_level: 4
keywords:
  - self-hosted
tags:
  - Self hosting
  - enable-nexus
---

:::tip SUPPORT, STABILITY, and DEPENDENCY INFO

Temporal Nexus 现已[普遍可用](/evaluate/development-product-features/release-stages#general-availability)。

:::

:::info NEW TO NEXUS?

本页介绍如何自行托管 Nexus。要了解 Nexus，请参阅[Nexus 工作原理页面](/Nexus)。要评估 Nexus 是否适合您的使用案例，请参阅[评估指南](/evaluate/Nexus)。

:::

## 启用Nexus

Nexus可以通过设置静态配置和动态配置条目进行配置。


:::note
Nexus 仅在单集群设置中受支持。有关操作详细信息，请参阅 [Nexus 架构](https://github.com/Temporalio/Temporal/blob/main/docs/architecture/Nexus.md)。
:::

:::note
将 `$PUBLIC_URL` 替换为外部调用者或集群内部可访问的 URL 值。
目前，外部 Nexus 调用被认为是实验性的，因此使用前端服务的内部负载平衡器的地址应该是安全的。
:::

要在您的部署中启用 Nexus：

1. 在服务器的静态配置中启用HTTP API。

   ```yaml
   services:
     frontend:
       rpc:
         # NOTE: keep other fields as they were
         httpPort: 7243

   clusterMetadata:
     # NOTE: keep other fields as they were
     clusterInformation:
       active:
         # NOTE: keep other fields as they were
         httpAddress: $PUBLIC_URL:7243
   ```

2. 设置所需的动态配置
    1. **1.30.X之前版本**需要设置公共回调地址和允许的回调地址。

**注意**：使用实验时应设置回调端点模板和允许的地址
       “外部”端点目标。

       ```yaml
       component.nexusoperations.callback.endpoint.template:
         # The URL must be publicly accessible if the callback is meant to be called by external services.
         # When using Nexus for cross namespace calls, the URL's host is irrelevant as the address is resolved using
         # membership. The URL is a Go template that interpolates the `NamepaceName` and `NamespaceID` variables.
         - value: https://$PUBLIC_URL:7243/namespaces/{{.NamespaceName}}/nexus/callback
       component.callbacks.allowedAddresses:
         # Limits which callback URLs are accepted by the server.
         # Wildcard patterns (*) and insecure (HTTP) callbacks are intended for development only.
         # For production, restrict allowed hosts and set AllowInsecure to false
         # whenever HTTPS/TLS is supported. Allowing HTTP increases MITM and data exposure risk.
         - value:
             - Pattern: "*" # Update to restrict allowed callers, e.g. "*.example.com"
               AllowInsecure: true # In production, set to false and ensure traffic is HTTPS/TLS encrypted
       ```

2. **版本1.30.X+**：Nexus 默认启用。只需要系统回调 URL。
       ```yaml
       component.nexusoperations.useSystemCallbackURL:
         - value: true
       ```

## 构建并使用 Nexus Services

- Elasticsearch v7 与 XPROT005X v1.7 及更高版本
- Elasticsearch v8 与 XPROT005X v1.18 及更高版本
- OpenSearch 2+ 与 XPROT005X v1.30.1 及更高版本
- MySQL v8.0.17 及更高版本以及 XPROT005X v1.20 及更高版本
- PostgreSQL v12 及更高版本以及 XPROT005X v1.20 及更高版本
- SQLite v3.31.0 及更高版本以及 XPROT005X v1.20 及更高版本

:::tip SDK GUIDES

- [开始](/develop/go/Nexus/feature-guide) |
  [Java](/develop/java/Nexus) |
  [Python](/develop/python/Nexus) |
  [TypeScript](/develop/typescript/Nexus) |
  [.NET](/develop/dotnet/Nexus)

:::
