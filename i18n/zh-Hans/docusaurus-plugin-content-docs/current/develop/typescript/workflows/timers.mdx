---
id: timers
title: Timers - TypeScript SDK
sidebar_label: Timers
slug: /develop/typescript/workflows/timers
toc_max_heading_level: 2
keywords:
  - timers
tags:
  - Workflows
  - Durable Timers
  - TypeScript SDK
  - Temporal SDKs
description: Workflow 使用 sleep() 或 Timer() 将耐用的 Timer 设置固定时间段。 Timer 是持久的，确保在停机后使用最少的资源继续执行。
---

## 什么是 Timer？ {#Timers}

Workflow 可以将 Timer 设置为固定时间段的耐用性。
在某些 SDK 中，该函数称为“sleep()”，而在其他 SDK 中，该函数称为“Timer()”。

Workflow 可以休眠数月。
Timer 是持久的，因此即使您的 Worker 或 Temporal 服务在时间段结束时关闭，只要您的 Worker 和 Temporal 服务备份，“sleep()”调用就会解析，您的代码将继续执行。

睡眠是一种轻资源操作：它不会占用进程，并且您可以在单个 Worker 上运行数百万个 Timer。

## TypeScript 中的异步设计模式 {#asynchronous-design-patterns}

“sleep”和“condition”的真正价值在于了解如何使用它们来建模异步业务逻辑。
以下是我们最常用的一些示例；如果您能想到的话，我们欢迎更多！

<详情>
<摘要>
赛车 Timers
</summary>

将“Promise.race”与 Timer 结合使用来动态调整延迟。

```ts
export async function processOrderWorkflow({
  orderProcessingMS,
  sendDelayedEmailTimeoutMS,
}: ProcessOrderOptions): Promise<void> {
  let processing = true;
  const processOrderPromise = processOrder(orderProcessingMS).then(() => {
    processing = false;
  });

  await Promise.race([processOrderPromise, sleep(sendDelayedEmailTimeoutMS)]);

  if (processing) {
    await sendNotificationEmail();
    await processOrderPromise;
  }
}
```

</details>
<详情>
<摘要>
赛车 Signals
</summary>

将“Promise.race”与 Signal 和触发器结合使用，以在系统时间或人工干预中较早的时间进行承诺解析。

```ts
import { defineSignal, sleep, Trigger } from '@temporalio/workflow';

const userInteraction = new Trigger<boolean>();
const completeUserInteraction = defineSignal('completeUserInteraction');

export async function yourWorkflow(userId: string) {
  setHandler(completeUserInteraction, () => userInteraction.resolve(true)); // programmatic resolve
  const userInteracted = await Promise.race([
    userInteraction,
    sleep('30 days'),
  ]);
  if (!userInteracted) {
    await sendReminderEmail(userId);
  }
}
```

您可以反转它以创建一个提醒模式，其中承诺在没有收到 Signal 的情况下得到解决。

:::caution Antipattern: Racing sleep.then

进行链式“睡眠”比赛时要小心。
这可能会导致错误，因为链接的“.then”仍将继续执行。

```ts
await Promise.race([
  sleep('5s').then(() => (status = 'timed_out')),
  somethingElse.then(() => (status = 'processed')),
]);

if (status === 'processed') await complete(); // takes more than 5 seconds
// status = timed_out
```

:::

</details>

<详情>
<摘要>
可更新Timer
</summary>

以下是如何使用“condition”构建可更新的 Timer：

```ts
import * as wf from '@temporalio/workflow';

// usage
export async function countdownWorkflow(): Promise<void> {
  const target = Date.now() + 24 * 60 * 60 * 1000; // 1 day!!!
  const timer = new UpdatableTimer(target);
  console.log('timer set for: ' + new Date(target).toString());
  wf.setHandler(setDeadlineSignal, (deadline) => {
    // send in new deadlines via Signal
    timer.deadline = deadline;
    console.log('timer now set for: ' + new Date(deadline).toString());
  });
  wf.setHandler(timeLeftQuery, () => timer.deadline - Date.now());
  await timer; // if you send in a signal with a new time, this timer will resolve earlier!
  console.log('countdown done!');
}
```

这可以在第三方包 [`Temporal-time-utils`](https://www.npmjs.com/package/Temporal-time-utils#user-content-updatableTimer) 中找到，您还可以在其中查看实现：

```ts
// implementation
export class UpdatableTimer implements PromiseLike<void> {
  deadlineUpdated = false;
  #deadline: number;

  constructor(deadline: number) {
    this.#deadline = deadline;
  }

  private async run(): Promise<void> {
    /* eslint-disable no-constant-condition */
    while (true) {
      this.deadlineUpdated = false;
      if (
        !(await wf.condition(
          () => this.deadlineUpdated,
          this.#deadline - Date.now(),
        ))
      ) {
        break;
      }
    }
  }

  then<TResult1 = void, TResult2 = never>(
    onfulfilled?: (value: void) => TResult1 | PromiseLike<TResult1>,
    onrejected?: (reason: any) => TResult2 | PromiseLike<TResult2>,
  ): PromiseLike<TResult1 | TResult2> {
    return this.run().then(onfulfilled, onrejected);
  }

  set deadline(value: number) {
    this.#deadline = value;
    this.deadlineUpdated = true;
  }

  get deadline(): number {
    return this.#deadline;
  }
}
```

</details>
