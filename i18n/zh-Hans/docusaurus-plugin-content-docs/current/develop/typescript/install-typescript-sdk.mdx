---
id: install-typescript-sdk
title: 安装 TypeScript SDK
description: 展示如何安装 TypeScript SDK
sidebar_label: 安装 TypeScript SDK
toc_max_heading_level: 3
tags:
  - TypeScript SDK
  - Temporal SDKs
---

## 如何安装 Temporal SDK {#install-a-Temporal-sdk}

[Temporal SDK](/encyclopedia/Temporal-sdks) 提供了一个框架
[Temporal应用程序](/Temporal#Temporal-应用程序)开发。

SDK 为您提供以下功能：

- [Temporal CLIent](/encyclopedia/Temporal-sdks#Temporal-client) 与
  [Temporal服务](/Temporal-服务)。
- 用于开发 [Workflows](/Workflows) 的 API。
- 用于创建和管理 [Worker 进程](/Workers#Worker) 的 API。
- 用于创作[活动](/Activity-定义)的API。

[![NPM](https://img.shields.io/npm/v/Temporalio.svg?style=for-the-badge)](https://www.npmjs.com/search?q=author%3ATemporal-sdk-team)

该项目需要 Node.js 18 或更高版本。

**创建一个项目**

```bash
npx @temporalio/create@latest ./your-app
```

**添加到现有项目**

```bash
npm install @temporalio/client @temporalio/worker @temporalio/workflow @temporalio/activity @temporalio/common
```

:::note

TypeScript SDK 的设计考虑了 TypeScript 优先的开发人员体验，但它同样适用于
JavaScript。

:::

### 如何查找 TypeScript SDK API 参考 {#api-reference}

Temporal TypeScript SDK API 参考已发布到 [typescript.Temporal.io](https://typescript.Temporal.io)。

### SDK 特定的代码示例在哪里？ {#代码示例}

您可以在以下位置找到可执行代码示例的完整列表
[Temporal 的 GitHub 存储库](https://github.com/Temporalio?q=samples-&type=all&language=&sort=)。

此外，一些[教程](https://learn.Temporal.io) 由完全可执行的模板支持
应用程序。

使用GitHub上存储的[TypeScript示例库](https://github.com/Temporalio/samples-typescript)进行演示
Temporal 的各种功能。

**哪里可以找到视频演示？**

[Temporal TypeScript YouTube 播放列表](https://www.youtube.com/playlist?list=PLl9kRkvFJrlTavecydpk9r6cF7qBmQJvb)。

### 如何导入 ECMAScript 模块 {#ecmascript-modules}

JavaScript 生态系统正在迅速朝着发布 ECMAScript 模块 (ESM) 而不是 CommonJS 模块的方向发展。对于
例如，“node-fetch@3”是 ESM，但“node-fetch@2”是 CommonJS。

有关导入纯 ESM 依赖项的更多信息，请参阅我们的
[Fetch ESM](https://github.com/Temporalio/samples-typescript/tree/main/fetch-esm) 示例以获取必要的配置
变化：

- `package.json` 必须包含 `"type": "module"` 属性。
- `tsconfig.json` 应以 `esnext` 格式输出。
- 导入必须包含“.js”文件扩展名。

## TypeScript 中的 Linting 和类型 {#linting-and-types}

如果您使用“@Temporalio/create”启动项目，那么您已经拥有我们推荐的 TypeScript 和 ESLint
配置。

如果您逐步将 Temporal 添加到现有应用程序中，我们建议您设置 linting 和类型，因为它们会有所帮助
在将错误交付生产之前就捕获它们，它们可以改善您的开发反馈循环。看看我们的
推荐的 [.eslintrc](https://github.com/Temporalio/samples-typescript/blob/main/.shared/.eslintrc.js) 文件和调整
以满足您的需求。
