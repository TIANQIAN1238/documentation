---
id: asynchronous-activity
title: 异步 Activity 完成 - Ruby SDK
sidebar_label: 异步Activity完成
description: 使用 Ruby SDK 在 Temporal 中异步完成 Activity。按照简单的步骤即可允许 Activity 函数返回，而无需 Activity 执行完成。
toc_max_heading_level: 4
keywords:
  - asynchronous activity completion
  - temporal
  - activity function
  - task token
  - workflow id
  - activity id
  - temporal client
  - external system completion
  - activity execution
  - complete
  - fail
  - cancel
  - get_async_activity_handle
  - ruby examples
tags:
  - Activities
  - Ruby SDK
  - Temporal SDKs
---

## 如何异步完成 Activity {#asynchronous-Activity-completion}

本页介绍如何异步完成 Activity。

[异步 Activity 完成](/Activity-execution#asynchronous-Activity-completion) 使 Activity 函数能够在 Activity 执行完成的情况下返回。

需要遵循三个步骤：

1. Activity 向外部系统提供完成 Activity 执行所需的识别信息。
   标识信息可以是[任务令牌](/Activity-execution#task-token)，也可以是 Namespace、Workflow Id 和 Activity Id 的组合。
2. Activity 函数以将其标识为等待外部系统完成的方式完成。
3. Temporal CLIent用于Heartbeat并完成Activity。

要将 Activity 标记为异步完成，请在 Activity 内执行以下操作。

```ruby
# Capture token for later completion
captured_token = Temporalio::Activity::Context.current.info.task_token

# Raise a special exception that says an activity will be completed somewhere else
raise Temporalio::Activity::CompleteAsyncError
```

对于 Activity 之外的 Update 和 Activity，请在客户端使用 [async_Activity_handle](https://ruby.Temporal.io/Temporalio/Client.html#async_Activity_handle-instance_method) 方法来获取 Activity 的句柄。

```ruby
handle = my_client.async_activity_handle(captured_token)
```

然后，在该句柄上，您可以对 Update 和 Activity 使用“heartbeat”、“complete”、“fail”或“report_cancellation”方法。

```ruby
handle.complete('completion value')
```
