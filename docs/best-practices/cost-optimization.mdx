---
title: "Workflow 成本优化"
sidebar_label: "成本优化"
description: "优化与 Temporal Cloud 上运行的工作负载相关的成本的策略，同时保持 Workflow 的可靠性和可观察性。"
toc_max_heading_level: 4
keywords:
  - cost optimization
  - actions
  - storage
  - pricing
  - best practices
tags:
  - Best Practices
  - Temporal Cloud
---

本指南提供了优化与 Temporal Cloud 上运行的工作负载相关的成本的策略，同时保持 Workflow 的可靠性和可观察性。

＃＃ 概述

Temporal Cloud 使用基于消耗的定价，其中包含两个主要成本组成部分：[操作和存储](/cloud/pricing#action)。
根据您的工作负载特征，优化机会会有很大差异 - 具有高 Signal 容量的 Workflow 与具有大负载的长期运行的 Workflow 面临着不同的成本驱动因素。

:::important
首先按照最佳实践构建 Workflow，然后根据观察到的成本进行优化。
过早的优化可能会损害可观测性并带来运营挑战。
:::

每次优化都涉及权衡。
本指南可帮助您根据您的具体要求就优化位置和方式做出明智的决策。

如果您需要有关 Workflow 设计注意事项的更多指导，请联系 Temporal 解决方案架构师。

<a id="overview"></a>
## 常见的反模式 {#overview}

避免这些模式，这些模式要么不必要地增加成本，要么通过积极的优化产生问题：

<a id="common-anti-patterns"></a>
### 过早的 Activity 整合 {#common-anti-patterns}

在了解故障模式之前组合活动会降低可观察性和重试控制。
应根据故障边界和重试要求来划分活动，而不仅仅是成本优化。
请参阅 [我应该在 Temporal Workflow 中使用多少个活动](https://temporal.io/blog/how-many-activities-should-i-use-in-my-temporal-workflow) 了解决策框架。

<a id="premature-activity-consolidation"></a>
### 不当使用本地活动 {#premature-activity-consolidation}

将本地活动用于所有操作，而不了解其故障语义和限制。
本地活动不提供 Worker 级别的隔离，并且具有不同的重试行为。
请参阅 [当地活动](/local-activity) 获取指导。

<a id="inappropriate-use-of-local-activities"></a>
### 缺少 Continue-As-New {#inappropriate-use-of-local-activities}

未实现 Continue-As-New 的长期运行的 Workflow 会积累大量事件历史记录，从而增加存储成本并影响性能。
Workflow 运行数天或数周或处理数千个事件需要 [Continue-As-New](/workflow-execution/continue-as-new)。

<a id="missing-continue-as-new"></a>
### Activity 重试次数较多 {#missing-continue-as-new}

一般来说，[Activity 重试](/encyclopedia/retry-policies) 的默认值相当不错。
但是，过多的 Activity 重试通常表明存在潜在问题，例如超时太短或活动频繁失败。
检测 Activity 重试频率，如果较高，请考虑在发生故障之前增加重试间隔或 Activity 超时。
有关重试默认值和模式的指南，请参阅 [恐怖故事：令人毛骨悚然的 Temporal 反模式](https://temporal.io/blog/spooky-stories-chilling-temporal-anti-patterns-part-2#2-hiding-behind-the-chainsaws)。

<a id="high-volume-of-activity-retries"></a>
### Workflow 历史中的大有效负载 {#high-volume-of-activity-retries}

当外部存储（S3、blob 存储）更合适时，通过 Workflow 传递多兆字节的有效负载。
对于大数据，请使用 [压缩](/troubleshooting/blob-size-limit-error#why-does-this-error-occur) 或 [索赔检查模式](https://dataengineering.wiki/Concepts/Software+Engineering/Claim+Check+Pattern)。

<a id="large-payloads-in-workflow-history"></a>
### 以牺牲可观察性为代价的过度优化 {#large-payloads-in-workflow-history}

积极优化成本，而无需维护足够的 Visibility 来满足调试和操作需求。
平衡成本降低与团队的可观察性要求。

例如，将五个单独的活动（验证输入、调用支付 API、Update 数据库、发送通知、生成收据）合并到单个“processOrder”Activity 中，操作数从 5 个减少到 1 个，但会丢失 Temporal UI 中的每步骤 Visibility。
当通知步骤失败时，您无法看到五个步骤中哪一个失败，您将失去独立的重试控制（通知失败会重试包括付款调用在内的整个流程），并且您无法按失败阶段过滤 Workflow。

同样，从长时间运行的数据处理 Activity 中删除 [心跳](/encyclopedia/detecting-activity-failures#activity-heartbeat) 可以保存操作，但这意味着您无法检测到卡住的 Worker，直到完整的 Activity 超时到期并且您失去进度跟踪（例如，“已处理 1,000 条记录中的 500 条记录”）。

<a id="over-optimization-at-the-expense-of-observability"></a>
### Activity 心跳过多 {#over-optimization-at-the-expense-of-observability}

每一次心跳都算作一次行动。
仅将心跳用于需要检测 Worker 故障并跟踪进度的长时间运行的活动（10 分钟以上）。
在几秒钟或几分钟内完成的短期运行活动不需要心跳。
请参阅 [Activity 心跳文档](/encyclopedia/detecting-activity-failures#which-activities-should-heartbeat) 获取指导。

<a id="excessive-activity-heartbeats"></a>
## 了解成本驱动因素 {#excessive-activity-heartbeats}

Temporal Cloud 定价由操作、存储和支持组成。
如果您是 Temporal Cloud 的新手，请参阅 [定价文件](/cloud/pricing#action) 以了解更多信息并熟悉 Temporal Cloud 中的 [什么会导致计费操作](/cloud/actions)。

<a id="understanding-cost-drivers"></a>
### 成本分配 {#understanding-cost-drivers}

对于大多数工作负载，操作占总成本的大部分，而存储通常占每月账单的 10% 或更少。
将优化工作重点放在特定工作负载的成本驱动因素上：

**高行动成本通常表明**：

- [Workflow 有许多活动](https://temporal.io/blog/how-many-activities-should-i-use-in-my-temporal-workflow)
- 频繁 [Signal、查询或 Update](/encyclopedia/workflow-message-passing#choosing-messages)
- 使用 [心跳](/cloud/worker-health#manage-worker-heartbeating) 进行长期活动
- 高[Activity 重试率](/develop/activity-retry-simulator)
- 广泛使用Query

**高存储成本通常表明**：

- Workflow 输入、输出或 Activity 结果中的大有效负载
- Workflow 体积大，保留时间长
- 长期运行的 Workflow，不带 Continue-As-New
- Workflows积累大量事件历史

<a id="cost-distribution"></a>
### 优化优先级 {#cost-distribution}

1. **行动优化**：通常提供最大的成本降低机会
2. **主动存储优化**：与长时间运行的 Workflow 或大负载相关
3. **保留存储优化**：与高容量和长保留期相关

<a id="optimization-priority"></a>
## 测量 {#optimization-priority}

在优化之前建立 [基线指标](https://docs.temporal.io/cloud/metrics/reference)，并确保在实施后验证影响。
具体来说：

- 操作消耗（根据 Workflow，每天/月，按 Namespace）
- 存储消耗（活动和保留）
- 每月费用（总计，每个 Namespace、每个 Workflow 类型）
- 可观察性指标（调试时间、事件检测）

<a id="measuring"></a>
## 动作优化 {#measuring}

[行动](https://docs.temporal.io/cloud/actions) 包含 Workflow 操作、Activity 执行、Signal、查询以及与 Temporal 的其他交互。
每个代表一个消耗单位。

<a id="actions-optimization"></a>
### Activity 粒度 {#actions-optimization}

Activity 粒度是影响成本和可观察性的基本架构决策。
更多活动提供更好的 Visibility 和重试控制，但会增加操作计数。
较少的活动降低了成本，但限制了可观察性。

有关此权衡的详细讨论，请参阅 [我应该在 Temporal Workflow 中使用多少个活动？](https://temporal.io/blog/how-many-activities-should-i-use-in-my-temporal-workflow)

<a id="activity-granularity"></a>
### Child Workflow 与活动 {#activity-granularity}

[Child Workflow 需要 2 次操作](/cloud/actions#child-workflows) 与 Activity 的 1 动作相比。
有关功能和用例的详细比较，请参阅 [Child Workflows 文档](/child-workflows)。

<a id="child-workflows-vs-activities"></a>
### 重试策略 {#child-workflows-vs-activities}

每次 Activity 重试都算作一次操作。
默认的 [重试策略](/encyclopedia/retry-policies) 可以是激进的，这适用于大多数操作，但对于昂贵的外部操作来说成本高昂。

例如，考虑调用第三方支付 API 的 Activity。
使用 Temporal 的默认 Retry Policy（1 秒初始间隔、2.0 退避系数、无限最大尝试次数），如果该 API 关闭 30 分钟，Activity 在达到 100 秒最大间隔上限之前会重试大约 20 次，然后继续每 100 秒重试一次。
每次重试算作 1 个操作。
在 1,000 个并发 Workflow 遭遇相同中断的情况下，仅重试就会产生 20,000 多个额外操作。

对于像这样昂贵的外部操作，请考虑：

- 设置 `MaximumAttempts` 限制总重试次数
- 增加`InitialInterval`（例如，增加到10秒）以减少重试频率
- 将错误类型添加到 `NonRetryableErrorTypes`，以解决重试时无法解决的错误（例如 4xx HTTP 状态代码）
- 使用 [下次重试延迟](/encyclopedia/retry-policies#per-error-next-retry-delay) 根据故障类型动态控制重试时间（例如，遵守速率限制标头）
- 实施 [Activity 暂停模式](/cli/activity#pause) 来等待手动干预而不是自动重试

使用 [Activity 重试模拟器](/develop/activity-retry-simulator) 可视化不同的 Retry Policy 配置如何影响重试行为和操作消耗。

请参阅有关 [掌握 Workflow 重试逻辑以实现弹性应用程序](https://temporal.io/blog/failure-handling-in-practice) 的博客文章以获取更多指导。

<a id="retry-policies"></a>
### 当地活动 {#retry-policies}

[本地 Activity](/local-activity#local-activity) 是 Activity 执行，它与生成它的 Workflow Execution 在同一进程中执行。
因此，连续运行的多个本地活动仅 [算作一次计费操作](/cloud/actions#activities)，而每个常规 Activity 都算作计费操作。
然而，将常规活动转换为本地活动需要权衡。
例如，如果特定的本地 Activity 失败，*所有*它们将一起重试。
查看 [文档](/local-activity) 或联系您的客户团队以了解更多信息。

<a id="local-activities"></a>
#### 何时坚持常规活动 {#local-activities}

如果您需要以下任何一项，请使用常规活动而不是本地活动：

- 活动可能需要 10 秒以上才能完成
- 每个Activity的独立重试控制
- 当不相关的活动失败时需要避免重新运行昂贵的活动
- 执行期间立即处理 Signal/Update
- 每个 Activity 的单独资源管理（如速率限制）

<a id="when-to-stick-with-regular-activities"></a>
### 批处理操作 {#when-to-stick-with-regular-activities}

<a id="batching-operations"></a>
#### Search Attributes {#batching-operations}

1. Workflow 开始时提供的 [Search Attributes](/search-attribute#custom-search-attribute) 不算作计费操作。
   如果在启动 Workflow 之前已知 Search Attribute 值，请在 Workflow 启动时提供这些值，以完全消除这些成本。
2. 对于在 Workflow Execution 期间必须为 Updated 的 Search Attribute，无论有多少个属性是 Updated，每个 `UpsertSearchAttributes` 调用都计为 1 个操作。
   将多个相关属性 Update 批处理为单个操作，以减少消耗的操作。

有关详细信息，请参阅 [Temporal Cloud 操作文档](/cloud/actions#workflows)。

<a id="search-attributes"></a>
#### Signal 处理 {#search-attributes}

在可行的情况下，在客户端实施重复数据删除逻辑或将数据聚合到更少的 Signal 中。
当使用 Signal 启动 Workflow 时，使用 `SignalWithStart` 而不是单独的 `StartWorkflow` 和 `SignalWorkflow` 调用。

<a id="signal-handling"></a>
## 存储优化 {#signal-handling}

[仓储成本](/cloud/pricing#storage) 分为活动存储（打开 Workflow）和保留存储（保留期间关闭 Workflow 历史记录）。
活动存储比保留存储昂贵得多。

<a id="storage-optimization"></a>
### 主动存储 {#storage-optimization}

Active Storage 适用于打开的 Workflow 及其事件历史记录。
以下部分详细介绍了 Active Storage 的优化机会。

<a id="active-storage"></a>
#### Continue-As-New {#active-storage}

对于具有延长睡眠/等待时间的长时间运行的 Workflow，在睡眠前调用 Continue-As-New 会关闭当前执行（移至更便宜的保留存储）并在工作恢复时重新开始，从而降低活动存储成本。
请参阅 [Continue-As-New 文档](/workflow-execution/continue-as-new) 了解更多信息。

＃＃＃＃ 压缩

大有效负载会增加 Active Storage 成本。
实现自定义 Data Converter，并针对中等大小的有效负载 (100KB-1MB) 进行压缩。
请参阅 [Data Converter 文档](/default-custom-data-converters#custom-data-converter) 了解更多信息。

<a id="continue-as-new"></a>
#### 声明检查模式 {#continue-as-new}

对于非常大的有效负载或二进制数据，请在外部存储数据（S3 或 GCS）并通过 Workflow 传递引用。

<a id="compression"></a>
### 保留存储 {#compression}

保留存储适用于保留期内关闭的 Workflow 历史记录。
以下部分详细介绍了保留存储的优化机会。

<a id="claim-check-pattern"></a>
#### 保留期限 {#claim-check-pattern}

默认 Namespace 保留期为 30 天（可在 1 到 90 天之间配置）。
根据运营和合规要求进行调整。

**注意事项**：

- 较短的保留时间可降低成本，但限制历史分析
- 在缩短保留时间之前审核调查模式
- 确保满足合规要求

有关配置详细信息，请参阅 [Namespace 保留文档](/temporal-service/temporal-server#retention-period)。

<a id="retained-storage"></a>
#### Workflow 导出 {#retained-storage}

Temporal Cloud 支持将 Workflow 历史记录导出到外部存储以确保合规性，同时保持更短的保留期。
请注意，Workflow 导出每次导出需要执行一次操作。

有关详细信息，请参阅 [Workflow 历史导出文档](/cloud/export)。
或者，如果您希望对封闭的 Workflow Execution 进行分析，请使用 [查看此博文，了解如何从导出的 Workflow 历史记录中获取见解](https://temporal.io/blog/get-insights-from-workflow-histories-export-on-temporal-cloud)。

＃＃ 验证

<a id="retention-periods"></a>
### 验证方法 {#retention-periods}

1. **非生产测试**：在生产部署之前验证功能正确性
2. **全面监控**：利用云UI中的[使用情况仪表板](/cloud/actions#usage)跟踪优化后对Actions和Storage的影响
3. **渐进式部署**：部署到一小部分，验证，然后扩展。查看 [Worker 版本控制文档](/production-deployment/worker-deployments/worker-versioning) 以了解如何对 Workflow 进行更改
4. **持续审查**：随着系统的发展，每季度重新评估优化效果

<a id="workflow-export"></a>
### 成功标准 {#workflow-export}

- 在不增加平均修复时间 (MTTR) 的情况下降低成本
- Workflow 成功率保持或提高
- 降低可观测性并不会增加平均事件检测时间 (MTTD)

＃＃＃ 工具

- Temporal Cloud 操作和存储指标的使用仪表板
- Workflow 按 Workflow 计费操作估算的历史记录
- 将指标导出到可观察平台（Datadog、Grafana 等）以进行自定义监控

<a id="validation"></a>
## 何时寻求帮助 {#validation}

当遇到以下情况时，请联系 Temporal 团队进行 Workflow 审核：

- 复杂的 Workflow 模式，优化路径不明确
- 合规性要求限制了优化选项
- 需要自定义数据转换器或高级模式
- 渴望专家验证优化策略

请联系您的 Temporal 客户代表或 [Temporal 支持](http://support.temporal.io) 讨论优化服务。
