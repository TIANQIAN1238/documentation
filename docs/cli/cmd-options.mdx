---
id: cmd-options
title: "Temporal CLI 命令选项参考"
sidebar_label: "命令选项"
description: Discover how to manage Temporal Workflows, from Activity Execution to Workflow Ids, using clusters, cron schedules,
  Discover how to manage Temporal Workflows, from Activity Execution to Workflow Ids, using clusters, cron schedules,
  dynamic configurations, and logging. Perfect for developers.
toc_max_heading_level: 4
keywords:
  - actions
  - active cluster
  - activity
  - activity execution
  - activity id
  - address
  - archival
  - backfill
  - batch job
  - build
  - build id
  - ca-certificate
  - calendar
  - certificate key
  - child workflows
  - cli reference
  - cluster
  - codec server
  - command-line-interface-cli
  - command-line-interface-cli,
  - concurrency control
  - configuration
  - context
  - continue-as-new
  - cron
  - cross-cluster-connection
  - data converters
  - endpoint
  - environment
  - event
  - event id
  - event type
  - events
  - external temporal and state events
  - failures
  - frontend
  - frontend address
  - frontend service
  - goroutine
  - grpc
  - history
  - http
  - interval
  - ip address
  - job id
  - log-feature
  - logging
  - logging and metrics
  - memo
  - metrics
  - namespace
  - namespace description
  - namespace id
  - namespace management
  - nondeterministic
  - notes
  - operation
  - operator
  - options-feature
  - overlap policy
  - pager
  - port
  - pragma
  - queries-feature
  - query
  - requests
  - reset point
  - resets-feature
  - retention policy
  - retries
  - reuse policy
  - schedule
  - schedule backfill
  - schedule id
  - schedule pause
  - schedule unpause
  - schedules
  - search attribute
  - search attributes
  - server
  - server options and configurations
  - sqlite
  - start-to-close
  - task queue
  - task queue type
  - temporal cli
  - temporal ui
  - time
  - time zone
  - timeout
  - timeouts and heartbeats
  - tls
  - tls server
  - uri
  - verification
  - visibility
  - web ui
  - workflow
  - workflow execution
  - workflow id
  - workflow run
  - workflow state
  - workflow task
  - workflow task failure
  - workflow type
  - workflow visibility
  - x509-certificate
tags:
  - Temporal CLI
---

<a id="active-cluster"></a>
## 活动集群 {#active-cluster}

活动集群名称。

## Activity-id {#activity-id}

标识 Activity 执行。

<a id="activity-jitter"></a>
## Activity-抖动 {#activity-jitter}

如果设置，Activity 将在指定抖动持续时间内的随机时间启动。

<a id="activity-type"></a>
## Activity型 {#activity-type}

命令适用于该类型的所有正在运行的活动。

## 地址 {#address}

Temporal 前端服务的主机和端口（格式为主机:端口）。

<a id="address"></a>
## API 密钥 {#api-key}

请求的 API 密钥。

<a id="api-key"></a>
## 已存档 {#archived}

列出已存档的 Workflow Execution。

:::note

注意：`--archived` 是实验性的。

:::

<a id="archived"></a>
## 构建 ID {#build-id}

标识要检索其可达性信息的构建。可以指定多次。

## 日历 {#calendar}

JSON `({"dayOfWeek":"Fri","hour":"17","minute":"5"})` 格式或 Cron 字符串形式的日历规范
`("30 2 \* \* 5" or "@daily")`。

<a id="build-id"></a>
## 追赶窗口 {#catchup-window}

服务器关闭时允许的最大追赶时间。

## 簇 {#cluster}

集群名称。

<a id="calendar"></a>
## 编解码器验证 {#codec-auth}

将请求的授权标头设置为 Codec Server。

<a id="catchup-window"></a>
## 编解码器端点 {#codec-endpoint}

远程 Codec Server 的端点。

## 颜色 {#color}

何时使用颜色：自动、始终、从不。 （默认值：自动）

<a id="cluster"></a>
## 命令超时 {#command-timeout}

命令执行超时。 0s表示没有超时。

<a id="codec-auth"></a>
## 并发 {#concurrency}

请求并发。

<a id="codec-endpoint"></a>
## 计划任务 {#cron}

Cron Schedule 的格式可以如下所示：

```text
┌───────────── minute (0 - 59)
│ ┌───────────── hour (0 - 23)
│ │ ┌───────────── day of the month (1 - 31)
│ │ │ ┌───────────── month (1 - 12)
│ │ │ │ ┌───────────── day of the week (0 - 6) (Sunday to Saturday)
│ │ │ │ │
* * * * *
```

## 数据 {#data}

Namespace 键=值格式的数据。

值必须采用 JSON 格式。

<a id="color"></a>
## 数据库文件名 {#db-filename}

用于保留 Temporal 状态的文件。默认情况下，当进程终止时，Workflow 会丢失。

## 深度 {#depth}

要获取和扩展的 Child Workflow 的数量。使用 `-1` 获取任意深度的 Child Workflow。

## 描述 {#description}

Namespace 描述或 Nexus 端点描述。您可以在 Nexus 端点描述中使用 Markdown 格式。

<a id="command-timeout"></a>
## 描述文件 {#description-file}

Nexus 端点描述文件的路径。描述文件的内容可以使用Markdown格式。

## 细节 {#detail}

提供了 Activity 失败的原因。

<a id="concurrency"></a>
## 试运行 {#dry-run}

模拟重置而不重置任何 Workflow Execution。

<a id="cron"></a>
## 动态配置值 {#dynamic-config-value}

动态配置值，格式为 `KEY=JSON_VALUE`。字符串值需要引号。

## 电子邮件 {#email}

所有者电子邮件。

<a id="data"></a>
## 启用连接 {#enable-connection}

启用跨集群连接。

<a id="db-filename"></a>
## 结束时间 {#end-time}

回填结束时间。

<a id="depth"></a>
## 环境 {#env}

从中读取环境变量的环境名称。

<a id="description"></a>
## 环境文件 {#env-file}

环境设置文件的路径。默认为 `$HOME/.config/temporalio/temporal.yaml`。

<a id="description-file"></a>
## 事件 ID {#event-id}

WorkflowTaskStarted 之后您想要重置为的任何事件的事件 ID（独占）。可以是WorkflowTaskCompleted，
WorkflowTaskFailed 或其他。

<a id="detail"></a>
## 排除文件 {#exclude-file}

指定要从重置中排除的 Workflow Execution 的输入文件。

<a id="dry-run"></a>
## 执行超时 {#execution-timeout}

[Workflow Execution](/workflow-execution) 的超时（以秒为单位），包括重试和 `ContinueAsNew` 任务。

<a id="dynamic-config-value"></a>
## 现有兼容构建 ID {#existing-compatible-build-id}

Task Queue 已知的版本集中已存在的构建 ID。新的构建 ID 存储在版本集中
包含此 Id，使它们与该集中的版本兼容。

<a id="email"></a>
## 字段 {#fields}

自定义要打印的字段。设置为“long”以自动打印更多主要字段。

<a id="enable-connection"></a>
## 首次执行运行 ID {#first-execution-run-id}

在以此运行 ID 开始的链中的最后一个执行上运行 Update。

## 折叠 {#fold}

将折叠 Child Workflow 的状态（这将减少获取和显示的信息数量）。
如果提供 `--no-fold`，则不区分大小写并忽略。

## 跟随 {#follow}

跟踪 Workflow Execution 的进度。

<a id="end-time"></a>
## 前端地址 {#frontend-address}

远程集群的前端地址。

## 全球的 {#global}

指示 Namespace 是否为全局 Namespace 的标志。

<a id="env"></a>
## gRPC-元 {#grpc-meta}

包含与请求一起发送的 gRPC 元数据（格式：`key=value`）。值必须采用有效的 JSON 格式。

##无头

禁用 Web 用户界面。

<a id="env-file"></a>
## 心跳超时 {#headless}

成功的 Worker 心跳之间允许的最大时间。

<a id="event-id"></a>
## 历史档案状态 {#heartbeat-timeout}

设置历史归档状态。有效值为“禁用”和“启用”。

<a id="exclude-file"></a>
## 历史 uri {#history-archival-state}

可以选择指定历史存档 URI（首次启用存档后无法更改）。

<a id="execution-timeout"></a>
## id 重用策略 {#history-uri}

允许在新的 Workflow Execution 中使用相同的 Workflow Id。选项：允许重复、仅允许重复失败、
拒绝重复，如果运行则终止。

## 身份 {#id-reuse-policy}

指定操作者的身份。

## 输入 {#identity}

使用 `--input` 命令选项在命令中包含数据。

此命令选项接受有效的 JSON 字符串。如果命令所作用的实体接受多个
参数，为 JSON 字符串中的 null 值传递“null”。

以下是使用 `--input` 命令选项启动 Workflow 的示例。此 Workflow 需要一个
字符串作为参数：

```shell
temporal workflow start --input '"+1 555-555-5555"'
```

<a id="existing-compatible-build-id"></a>
## 输入文件 {#input}

从 JSON 文件传递​​ Workflow 的可选输入。如果有多个JSON文件，将它们连接起来并分开
通过空格或换行符。命令行输入将覆盖文件输入。

<a id="fields"></a>
## 输入并行性 {#input-file}

并行运行的 goroutine 数量。每个 goroutine 每秒处理一行。

<a id="first-execution-run-id"></a>
## 输入分隔符 {#input-parallelism}

输入文件的分隔符。默认为选项卡 (`\t`)。

## 间隔 {#input-separator}

间隔持续时间，例如 90m 或 90m/13m 以包括相位偏移。

<a id="fold"></a>
## ip {#interval}

将前端服务绑定到的 IPv4 地址。 （默认：127.0.0.1）

<a id="follow"></a>
## 抖动 {#ip}

抖动持续时间。

<a id="frontend-address"></a>
## 职位 ID {#jitter}

批处理作业 ID。

<a id="global"></a>
## 保持暂停 {#job-id}

如果提供此标志并且 Activity 已暂停，则复位后它将保持暂停状态。

## 限制 {#keep-paused}

一页上要打印的项目数。

<a id="grpc-meta"></a>
## 日志格式 {#limit}

设置日志格式。选项：[“json”，“漂亮”]。

<a id="headless"></a>
## 日志级别 {#log-format}

设置日志级别。选项：[“调试”“信息”“警告”“错误”“致命”]。

<a id="heartbeat-timeout"></a>
## 全部匹配 {#log-level}

如果设置，所有当前正在运行的活动都将取消暂停。

<a id="history-archival-state"></a>
## 最大字段长度 {#match-all}

每个属性字段的最大长度。

<a id="history-uri"></a>
## 最大组数 {#max-field-length}

限制返回的兼容集数量。指定 1 仅返回当前默认主要版本集。 0
返回所有集合。

<a id="id-reuse-policy"></a>
## 备忘录 {#max-sets}

在 Schedule 上设置备忘录（格式：键=值）。使用有效的 JSON 格式作为值。

<a id="identity"></a>
## 备忘录文件 {#memo}

从文件中设置备忘录。每行应遵循格式 key=value。使用有效的 JSON 格式作为值。

<a id="input"></a>
## 指标端口 {#memo-file}

`/metrics` 的端口。默认情况下启用并随机分配端口。

## 姓名 {#metrics-port}

远程集群的前端地址或端点名称。

<a id="input-file"></a>
## Namespace {#name}

标识 Temporal Workflow 中的 Namespace。

<a id="input-parallelism"></a>
## Namespace-id {#namespace}

Namespace ID。

<a id="input-separator"></a>
## 不折叠 {#namespace-id}

禁用折叠。将获取并显示设置深度内的所有 Child Workflow。

<a id="interval"></a>
## 无 json-shorthand-payloads {#no-fold}

原始负载输出，即使使用了 JSON 选项。

<a id="ip"></a>
## 无寻呼机 {#no-json-shorthand-payloads}

禁用交互式寻呼机。

<a id="jitter"></a>
## 不确定性 {#no-pager}

仅当最后一个事件是 `WorkflowTaskFailed` 且存在不确定性错误时，才重置 Workflow Execution。

<a id="job-id"></a>
## 注释 {#non-deterministic}

注释字段的初始值。

## 输出 {#notes}

输出的格式。选项：表、json、卡片。

<a id="keep-paused"></a>
## 重叠策略 {#output}

重叠政策。选项：跳过、BufferOne、BufferAll、取消其他、终止其他、允许全部。

## 寻呼机 {#overlap-policy}

设置 Temporal CLI 使用的寻呼机。选项有 less、more 和 favoritePager。

## 暂停 {#pager}

暂停 Schedule。

<a id="limit"></a>
## 失败时暂停 {#pause}

发生任何 Workflow 故障后暂停 Schedule。

## 港口 {#pause-on-failure}

前端 gRPC 服务的端口。

<a id="log-format"></a>
## 推广全球 {#port}

将本地 Namespace 升级为全局 Namespace。

<a id="log-level"></a>
## Query {#promote-global}

提供类似 SQL 的 Search Attribute 的 Query，以返回要重置的 Workflow Execution。欲了解更多信息，请参阅
[`Temporal Workflow list`](/cli/workflow#list) 命令。

## 生的 {#query}

以 JSON 格式打印原始数据。对于脚本编写，我们建议使用此选项而不是 `-o json`。

<a id="match-all"></a>
## 可达性类型 {#raw}

指定您希望如何过滤构建 ID 的可达性。以下是有效的选择：

- `open`：可由一个或多个打开的 Workflow 访问。
- `closed`：可由一个或多个封闭的 Workflow 到达。
- `existing`：可通过打开或关闭的 Workflow 到达。

始终报告新 Workflow 可访问的构建 ID。

<a id="max-field-length"></a>
## 重新申请类型 {#reachability-type}

重置点后重新应用的事件类型。选项：Signal，无。

## 原因 {#reapply-type}

操作原因。

<a id="max-sets"></a>
## 拒绝条件 {#reason}

用于拒绝基于 Workflow 状态的查询的可选标志。有效值为“not_open”和“not_completed_cleanly”。

<a id="memo"></a>
## 剩余操作 {#reject-condition}

允许的操作总数。

<a id="memo-file"></a>
## 重置尝试 {#remaining-actions}

提供此标志将重置尝试次数。

<a id="metrics-port"></a>
## 重置心跳 {#reset-attempts}

提供此标志将重置心跳详细信息。

<a id="name"></a>
## 重置点 {#reset-heartbeat}

仅显示符合重置条件的 Workflow 事件。

## 结果 {#reset-points}

设置Activity完成的结果值。

## 保留 {#result}

Workflow Execution 保留。

<a id="namespace"></a>
## 重试退避系数 {#retention}

用于计算下一次重试间隔的系数。下一个重试间隔是上一个间隔乘以
系数。必须为 1 或更大。

<a id="namespace-id"></a>
## 重试初始间隔 {#retry-backoff-coefficient}

第一次重试的时间间隔。如果 retryBackoffCoefficient 为 1.0，则它将用于所有重试。

<a id="no-fold"></a>
## 重试最大尝试次数 {#retry-initial-interval}

最大尝试次数。当超过时，即使尚未过期，重试也会停止。 1 禁用重试。 0 表示
无限制（直到超时）。

<a id="no-json-shorthand-payloads"></a>
## 重试最大间隔 {#retry-maximum-attempts}

重试之间的最大间隔。指数退避导致间隔增加。该值是增量的上限。
默认值为初始间隔的 100 倍。

<a id="no-pager"></a>
## 运行 ID {#retry-maximum-interval}

标识当前 Workflow 运行。

<a id="non-deterministic"></a>
## 运行超时 {#run-id}

单次 Workflow 运行的超时（以秒为单位）。

<a id="notes"></a>
## Schedule-id {#run-timeout}

Schedule ID。

<a id="output"></a>
## Schedule-关闭超时 {#schedule-id}

指示调用者愿意等待 Activity 完成的时间。限制重试的时间长度。

<a id="overlap-policy"></a>
## Schedule-启动超时 {#schedule-to-close-timeout}

限制 Activity 任务在 Worker 拾取它之前可以在 Task Queue 中停留的时间。此超时始终不可重试，
因为重试所能实现的只是将其放回到同一个队列中。默认为 `Schedule_to_close_timeout` 或 Workflow
如果未指定则执行超时。

<a id="pager"></a>
## 搜索属性 {#schedule-to-start-timeout}

在 Schedule 上设置 Search Attribute（格式为 `key=value`）。使用有效的 JSON 格式作为值。

<a id="pause"></a>
## 设置为默认值 {#search-attribute}

设置后，将目标兼容集设置为 Task Queue 的默认设置。

如果当前默认值是不同的集，则目标集将替换它。

<a id="pause-on-failure"></a>
## 跳过基础不是当前的 {#set-as-default}

如果基本 Workflow 运行不是当前的 Workflow 运行，则跳过 Workflow Execution。

<a id="port"></a>
## 跳过当前打开 {#skip-base-is-not-current}

如果当前运行针对与基本运行相同的 Workflow Id 打开，则跳过 Workflow Execution。

<a id="promote-global"></a>
## sqlite 编译指示 {#skip-current-open}

以 pragma=value 格式指定 sqlite pragma 语句。编译指示选项：[“journal_mode”“同步”]。

<a id="query"></a>
## 启动延迟 {#sqlite-pragma}

指定 Workflow 启动之前的延迟。

<a id="raw"></a>
## 开始时间 {#start-delay}

回填开始时间。

<a id="reachability-type"></a>
## 开始到关闭超时 {#start-time}

Activity 在被 Worker 选取后允许执行的最长时间。此超时始终可重试。

<a id="reapply-type"></a>
## 目标-Namespace {#start-to-close-timeout}

Namespace，其中处理程序 Worker 将轮询 Nexus 任务。

<a id="reason"></a>
## 目标任务队列 {#target-namespace}

Task Queue，其中处理程序 Worker 将轮询 Nexus 任务。

<a id="reject-condition"></a>
## 目标网址 {#target-task-queue}

Nexus 请求转发到的外部 Nexus 端点。可用作 `--target-Namespace` 的替代品
和`--target-task-queue`。

:::note

注意：`--target-url` 是实验性的。

:::

<a id="remaining-actions"></a>
## 任务队列 {#target-url}

Task Queue。

<a id="reset-attempts"></a>
## 任务队列类型 {#task-queue}

Task Queue 类型，可以是 Workflow 或 Activity。默认类型为 Workflow。

<a id="reset-heartbeat"></a>
## 任务超时 {#task-queue-type}

Workflow 任务的开始到关闭超时（以秒为单位）。

<a id="reset-points"></a>
## 时间格式 {#task-timeout}

时间格式为：relative、iso、raw。

<a id="result"></a>
## 时区 {#time-format}

时区（IANA 名称）。

<a id="retention"></a>
## tls {#time-zone}

启用 TLS 加密，无需附加选项（例如 mTLS 或客户端证书）。

<a id="retry-backoff-coefficient"></a>
## tls-ca-数据 {#tls}

服务器 CA 证书的数据。不能与 --tls-ca-path 一起使用。

<a id="retry-initial-interval"></a>
## tls-ca-路径 {#tls-ca-data}

服务器 CA 证书的路径。

<a id="retry-maximum-attempts"></a>
## tls-证书数据 {#tls-ca-path}

x509 证书的数据。不能与 --tls-cert-path 一起使用。

<a id="retry-maximum-interval"></a>
## tls 证书路径 {#tls-cert-data}

x509 证书的路径。

<a id="run-id"></a>
## tls-禁用主机验证 {#tls-cert-path}

禁用 TLS 主机名验证。

<a id="run-timeout"></a>
## tls-键-数据 {#tls-disable-host-verification}

私有证书密钥数据。不能与 --tls-key-path 一起使用。

<a id="schedule-id"></a>
## tls 密钥路径 {#tls-key-data}

私有证书密钥的路径。

<a id="schedule-to-close-timeout"></a>
## tls-服务器名称 {#tls-key-path}

覆盖目标 TLS 服务器名称。

## 类型 {#tls-server-name}

Search Attribute型。选项：文本、关键字、整数、双精度、布尔值、日期时间、关键字列表。

<a id="schedule-to-start-timeout"></a>
## 用户界面资产路径 {#type}

UI 自定义资源路径。

<a id="search-attribute"></a>
## ui-编解码器-端点 {#ui-asset-path}

UI 远程 Data Converter HTTP 端点。

<a id="set-as-default"></a>
## 用户界面 IP {#ui-codec-endpoint}

Web UI 绑定到的 IPv4 地址。

<a id="skip-base-is-not-current"></a>
## 用户界面端口 {#ui-ip}

Web UI 的端口。默认值：`--port` + 1000（例如 4000）。

<a id="skip-current-open"></a>
## 取消暂停 {#ui-port}

取消暂停 Schedule。

<a id="sqlite-pragma"></a>
## 取消设置描述 {#unpause}

取消设置描述。

<a id="start-delay"></a>
## 详细 {#unset-description}

打印应用的 Namespace 更改。

<a id="start-time"></a>
## Visibility-存档状态 {#verbose}

Visibility 存档状态。有效值：“禁用”、“启用”。

<a id="start-to-close-timeout"></a>
## Visibility-uri {#visibility-archival-state}

指定 Visibility 存档的 URI。启用存档后无法更改。

<a id="target-namespace"></a>
## Workflow-id {#visibility-uri}

Workflow ID。

<a id="target-task-queue"></a>
## Workflow型 {#workflow-id}

Workflow 类型名称。

## 是的 {#workflow-type}

确认所有提示。
