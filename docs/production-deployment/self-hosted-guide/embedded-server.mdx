---
id: embedded-server
title: "将 Temporal Server 嵌入为 Go 库"
sidebar_label: "嵌入式服务器"
description: "将 Temporal Server 作为嵌入式 Go 库运行以进行测试和开发。了解如何使用 Temporal.NewServer() 在进程内运行 Temporal Server。"
slug: /self-hosted-guide/embedded-server
toc_max_heading_level: 4
keywords:
  - embedded
  - library
  - testing
  - development
  - go
tags:
  - Temporal Service
  - Self-hosting
  - Go SDK
---

您可以将 Temporal Server 作为嵌入式 Go 库运行，而不是将其部署为单独的服务。
此方法对于您想要在进程内运行 Temporal 而不管理外部基础设施的测试和开发场景非常有用。

:::caution Not for production use

使用 SQLite 的嵌入式部署仅适合**测试和开发**。
对于生产工作负载，使用 [MySQL、PostgreSQL 或 Cassandra](/temporal-service/persistence) 作为持久层将 Temporal 部署为服务。

:::

<a id="reference-implementation"></a>
## 参考实现 {#reference-implementation}

运行嵌入式 Temporal Server 的推荐方法是使用 Temporal CLI 的开发服务器实现作为参考。
CLI 的 [开发服务器包](https://github.com/temporalio/cli/tree/main/internal/devserver) 提供了一个完整的实现，可以处理：

- SQLite 配置和模式设置
- Namespace 创作
- 服务配置
- 端口分配

您可以针对您自己的嵌入式用例研究和调整此实现。

<a id="basic-server-api"></a>
## 基本服务器API {#basic-server-api}

嵌入Temporal的核心API是`Temporal.NewServer()`：

```go
import (
    "go.temporal.io/server/temporal"
    "go.temporal.io/server/common/config"
)

server, err := temporal.NewServer(
    temporal.ForServices(temporal.DefaultServices),
    temporal.WithConfig(cfg),
    temporal.InterruptOn(temporal.InterruptCh()),
)
if err != nil {
    log.Fatal(err)
}

if err := server.Start(); err != nil {
    log.Fatal(err)
}
```

挑战在于正确构建 `config.Config` 结构，特别是对于 SQLite 来说，它需要：

1. **架构设置** - SQLite 数据库需要通过 `sqliteschema.SetupSchema()` 进行架构初始化
2. **Namespace 创建** - Namespace 可以通过 `sqliteschema.CreateNamespaces()` 预先创建
3. **服务配置** - 所有四个服务（前端、历史、匹配、Worker）都需要正确的端口配置

<a id="configuration-from-file"></a>
## 文件中的配置 {#configuration-from-file}

对于非 SQLite 数据库，您可以从 YAML 文件加载配置：

```go
cfg, err := config.Load(
    config.WithConfigFile("/path/to/config.yaml"),
)
if err != nil {
    log.Fatal(err)
}

server, err := temporal.NewServer(
    temporal.ForServices(temporal.DefaultServices),
    temporal.WithConfig(cfg),
)
```

或者从包含特定于环境的文件的目录加载：

```go
cfg, err := config.Load(
    config.WithConfigDir("./config"),
    config.WithEnv("development"),
)
```

<a id="server-options-reference"></a>
## 服务器选项参考 {#server-options-reference}

`Temporal.NewServer()` 函数接受自定义服务器的选项。
完整列表请参见 [服务器选项参考](/references/server-options)。

关键选项包括：

| 选项 | 描述 |
|--------|-------------|
| `ForServices([]string)` | 要运行的服务（默认：前端、历史记录、匹配、Worker） |
| `WithConfig(*config.Config)` | 服务器配置 |
| `WithLogger(log.Logger)` | 自定义记录器 |
| `WithAuthorizer(authorization.Authorizer)` | 定制授权 |
| `WithClaimMapper(func)` | 身份验证的角色/声明映射 |
| `WithCustomMetricsHandler(metrics.Handler)` | 自定义指标处理程序 |
| `WithDynamicConfigClient(dynamicconfig.Client)` | 运行时配置 |
| `InterruptOn(chan)` | 优雅关闭的通道 |

<a id="sqlite-limitations"></a>
## SQLite 限制 {#sqlite-limitations}

SQLite 仅用于测试和开发：

- **单写入器**：SQLite 一次仅支持一名写入器，限制了写入吞吐量
- **内存模式没有持久性**：内存模式在重新启动时会丢失数据
- **不可扩展**：无法处理生产工作负载
- **单分片**：对 SQLite 使用 `NumHistoryShards: 1`

对于生产，请使用 MySQL、PostgreSQL 或 Cassandra 以及适当扩展的多节点部署。

<a id="examples"></a>
## 示例 {#examples}

有关完整的工作示例，请参阅：

- [Temporal CLI 开发服务器](https://github.com/temporalio/cli/tree/main/internal/devserver) - SQLite 嵌入的参考实现
- [样本服务器存储库](https://github.com/temporalio/samples-server) - 服务器扩展性示例：
  - [授权人](https://github.com/temporalio/samples-server/tree/main/extensibility/authorizer) - 自定义授权和声明映射
  - [指标处理程序](https://github.com/temporalio/samples-server/tree/main/extensibility/metrics-handler) - 自定义指标处理
  - [传输层安全协议](https://github.com/temporalio/samples-server/tree/main/tls) - 用于安全通信的 TLS 配置
  - [Docker 组合](https://github.com/temporalio/samples-server/tree/main/compose) - 数据库配置（PostgreSQL、MySQL、Cassandra）

＃＃ 有关的

- [服务器选项参考](/references/server-options)
- [部署](/self-hosted-guide/deployment)
- [Visibility 存储](/self-hosted-guide/visibility)
