---
id: upgrade-server
title: "升级Temporal Server"
sidebar_label: "升级服务器"
description: Upgrade your Temporal Server effectively. Follow our step-by-step guide for seamless upgrades, ensuring data
  Upgrade your Temporal Server effectively. Follow our step-by-step guide for seamless upgrades, ensuring data
  compatibility and schema alignment.
slug: /self-hosted-guide/upgrade-server
toc_max_heading_level: 4
keywords:
  - how-to
tags:
  - Temporal Service
  - Self-hosting
---

## 如何升级Temporal Server版本 {#upgrade-server}

如果有更新版本的 [Temporal Server](/temporal-service/temporal-server) 可用，则会在
Temporal 网页用户界面。

:::info

如果您使用的版本早于 1.0.0，请通过以下方式联系我们
[社区.Temporal.io](http://community.temporal.io) 请问如何升级。

:::

首先检查您要升级到的版本是否需要升级数据库架构。如果一个
需要升级数据库模式，会直接在
[发行说明](https://github.com/temporalio/temporal/releases)。有些版本需要更改架构，有些版本需要更改架构
不。我们确保任何连续版本在数据库架构升级、功能和功能方面都是兼容的
系统行为；但是，不能保证_任何_两个非连续版本之间存在兼容性。

<a id="key-considerations"></a>
### 关键考虑因素 {#key-considerations}

升级 Temporal Server 时，需要牢记两个关键注意事项：

1. **顺序升级：** Temporal Server 应顺序升级。也就是说，如果您使用的是版本 \(v1.n.x\)，
   您的下一次升级应该是 \(v1.n+1.x\) 或最接近的可用后续版本。这个顺序应该是
   重复直到达到您想要的版本。

2. **数据兼容性：** 在升级过程中，Temporal Server 要么 Update 要么重构现有版本
   数据以匹配新版本的数据格式。 Temporal Server 仅确保两个之间的向后兼容性
   连续的小版本。因此，在升级过程中跳过版本可能会导致旧的数据格式变得更旧。
   不可读。如果以前的数据格式无法解释并转换为新格式，则升级过程
   将会不成功。

<a id="step-by-step-upgrade-procedure"></a>
### 逐步升级过程： {#step-by-step-upgrade-procedure}

升级 Temporal Server 需要有条不紊的方法来确保数据完整性、兼容性和无缝性
版本之间的过渡。以下文档概述了成功升级您的系统的分步过程
Temporal Server。

升级 Temporal Server 版本时，请确保按顺序升级。

1. **升级数据库架构：** 在启动 Temporal Server 升级之前，请使用推荐的升级工具之一
   Update 您的数据库架构。这可确保它与您想要升级到的 Temporal Server 版本保持一致。
2. **升级 Temporal Server：** 数据库架构为 Updated 后，继续升级 Temporal Server 部署
   到下一个连续版本。
3. **迭代升级**（可选）：迭代地继续此过程（步骤 1 和 2），直到达到所需的目标
   Temporal Server版本。

通过遵守上述准则并遵循分步程序，您可以确保顺利成功
升级您的 Temporal Server。

Temporal Server 升级 Update 或使用新版本中引入的格式重写旧版本数据。
因为 Temporal Server 保证两个连续的次要版本之间的向后兼容性，并且因为较旧的
代码的版本最终会从代码库中删除，升级时跳过版本可能会导致旧版本
格式变得无法识别。如果旧格式的数据无法读取并重写为新格式，则
升级失败。

检查 [Temporal Server 发布](https://github.com/temporalio/temporal/releases) 并遵循以下版本
订单。您可以跳过补丁版本；升级时请使用小版本的最新补丁。

另外，请注意，每次升级都需要历史服务加载所有分片和 Update 分片元数据，因此允许
每个版本大约需要 10 分钟才能完成这些过程，然后再升级到下一个版本。

使用升级工具之一升级数据库架构，使其与 Temporal Server 版本兼容
升级为.

<a id="upgrade-cassandra-schema"></a>
### 升级 Cassandra 架构 {#upgrade-cassandra-schema}

如果您使用 Cassandra 来实现 Temporal 服务的持久性，请使用 `Temporal-cassandra-tool` 来升级两者
默认 Persistence 和 Visibility 模式。

**默认架构升级示例：**

```bash
temporal_v1.2.1 $ temporal-cassandra-tool \
   --tls \
   --tls-ca-file <...> \
   --user <cassandra-user> \
   --password <cassandra-password> \
   --endpoint <cassandra.example.com> \
   --keyspace temporal \
   --timeout 120 \
   update \
   --schema-dir ./schema/cassandra/temporal/versioned
```

**示例 Visibility 架构升级：**

```bash
temporal_v1.2.1 $ temporal-cassandra-tool \
   --tls \
   --tls-ca-file <...> \
   --user <cassandra-user> \
   --password <cassandra-password> \
   --endpoint <cassandra.example.com> \
   --keyspace temporal_visibility \
   --timeout 120 \
   update \
   --schema-dir ./schema/cassandra/visibility/versioned
```

<a id="upgrade-elasticsearch-schema"></a>
### 升级 Elasticsearch 架构 {#upgrade-elasticsearch-schema}

如果您将 Elasticsearch 用于 Temporal 服务的 Visibility，请使用 `Temporal-elasticsearch-tool` 进行升级
架构。

**架构升级示例：**

```bash
echo "Updating index mappings: $ES_VISIBILITY_INDEX"
temporal-elasticsearch-tool \
	--endpoint "$ES_SCHEME://$ES_HOST:$ES_PORT" \
	--user "$ES_USER" \
	--password "$ES_PWD" \
	update-schema \
	--index "$ES_VISIBILITY_INDEX"
```

<a id="upgrade-postgresql-or-mysql-schema"></a>
### 升级 PostgreSQL 或 MySQL 架构 {#upgrade-postgresql-or-mysql-schema}

如果您使用 MySQL 或 PostgreSQL，请使用 `Temporal-sql-tool`，其工作原理与
`Temporal-cassandra-tool`。

请参阅此 [生成文件](https://github.com/temporalio/temporal/blob/v1.4.1/Makefile#L367-L383) 了解上下文。

#### PostgreSQL

**默认架构升级示例：**

```bash
./temporal-sql-tool \
	--tls \
	--tls-enable-host-verification \
	--tls-cert-file <path to your client cert> \
	--tls-key-file <path to your client key> \
	--tls-ca-file <path to your CA> \
	--ep localhost -p 5432 -u temporal -pw temporal --pl postgres --db temporal update-schema -d ./schema/postgresql/v96/temporal/versioned
```

**示例 Visibility 架构升级：**

```bash
./temporal-sql-tool \
	--tls \
	--tls-enable-host-verification \
	--tls-cert-file <path to your client cert> \
	--tls-key-file <path to your client key> \
	--tls-ca-file <path to your CA> \
	--ep localhost -p 5432 -u temporal -pw temporal --pl postgres --db temporal_visibility update-schema -d ./schema/postgresql/v96/visibility/versioned
```

如果您要将 PostgreSQL 升级到 v12 或更高版本，以通过 Temporal Server v1.20 启用高级 Visibility 功能，
先升级你的PostgreSQL版本，然后使用`postgres12`插件运行`Temporal-sql-tool`，如下所示
以下示例：

```bash
./temporal-sql-tool \
	--tls \
	--tls-enable-host-verification \
	--tls-cert-file <path to your client cert> \
	--tls-key-file <path to your client key> \
	--tls-ca-file <path to your CA> \
	--ep localhost -p 5432 -u temporal -pw temporal --pl postgres12 --db temporal_visibility update-schema -d ./schema/postgresql/v12/visibility/versioned
```

#### MySQL

**默认架构升级示例：**

```bash
./temporal-sql-tool \
	--tls \
	--tls-enable-host-verification \
	--tls-cert-file <path to your client cert> \
	--tls-key-file <path to your client key> \
	--tls-ca-file <path to your CA> \
	--ep localhost -p 3036 -u root -pw root --pl mysql --db temporal update-schema -d ./schema/mysql/v57/temporal/versioned/
```

**示例 Visibility 架构升级：**

```bash
./temporal-sql-tool \
	--tls \
	--tls-enable-host-verification \
	--tls-cert-file <path to your client cert> \
	--tls-key-file <path to your client key> \
	--tls-ca-file <path to your CA> \
	--ep localhost -p 3036 -u root -pw root --pl mysql --db temporal_visibility update-schema -d ./schema/mysql/v57/visibility/versioned/
```

如果您要将 MySQL 升级到 v8.0.17 或更高版本以通过 Temporal Server v1.20 启用高级 Visibility 功能，请升级
首先使用您的 MySQL 版本，然后使用 `mysql8` 插件运行 `Temporal-sql-tool`，如下例所示：

```bash
./temporal-sql-tool \
	--tls \
	--tls-enable-host-verification \
	--tls-cert-file <path to your client cert> \
	--tls-key-file <path to your client key> \
	--tls-ca-file <path to your CA> \
	--ep localhost -p 5432 -u temporal -pw temporal --pl mysql8 --db temporal_visibility update-schema -d ./schema/mysql/v8/visibility/versioned.
```

<a id="roll-out-technique"></a>
### 推出技术 {#roll-out-technique}

我们建议准备暂存 Temporal 服务，然后执行以下操作来验证升级是否成功：

1. 在暂存 Temporal 服务上创建一些模拟负载。
2. 升级暂存 Temporal 服务中的数据库架构。
3. 等待并观察几分钟，以验证服务器和客户端均没有出现不稳定的行为
   模拟加载逻辑。
4. 升级服务器。
5. 现在对实时环境 Temporal 服务执行相同的操作。
