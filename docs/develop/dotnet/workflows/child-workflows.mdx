---
id: child-workflows
title: "Child Workflows - .NET SDK"
sidebar_label: "Child Workflows"
description: "启动子 Workflow Execution 并使用 Temporal .NET SDK 设置父关闭策略。发现 ExecuteChildWorkflowAsync 等方法并管理 Workflow 行为。"
keywords:
  - start child workflow execution
  - temporal .net sdk
  - child workflow api
  - child workflow events
  - startchildworkflowexecutioninitiated
  - childworkflowexecutionstarted
  - childworkflowexecutioncompleted
  - workflow execution event history
  - parent close policy
  - executechildworkflowasync
  - startchildworkflowasync
  - workflow run id
  - signal child workflow
  - temporal .net examples
  - manage child workflows
  - child workflow options
  - parentclosepolicy
  - child workflow in .net
tags:
  - Workflows
  - .Net SDK
  - Temporal SDKs
  - Child Workflows
---

此页面显示如何执行以下操作：

- [开始一个孩子 Workflow Execution](#child-workflows)
- [设置父关闭策略](#parent-close-policy)

<a id="child-workflows"></a>
## 启动一个子 Workflow Execution {#child-workflows}

[儿童 Workflow Execution](/child-workflows) 是一个 Workflow Execution，它是使用 Child Workflow API 来自另一个 Workflow 的 Scheduled。

使用 Child Workflow API 时，Child Workflow 相关事件（[StartChildWorkflowExecutionInitiated](/references/events#startchildworkflowexecutioninitiated)、[子WorkflowExecutionStarted](/references/events#childworkflowexecutionstarted)、[ChildWorkflowExecutionCompleted](/references/events#childworkflowexecutioncompleted) 等）将记录在 Workflow Execution Event History 中。

始终阻止进度，直到 [子WorkflowExecutionStarted](/references/events#childworkflowexecutionstarted) 事件记录到 Event History，以确保子 Workflow Execution 已启动。
此后，可以使用 Child Workflow 选项中设置的 _Abandon_ [家长关闭政策](/parent-close-policy) 放弃子 Workflow Execution。

要在 .NET 中生成子 Workflow Execution，请使用 `ExecuteChildWorkflowAsync()` 方法来启动 Child Workflow 并等待完成或
使用 `StartChildWorkflowAsync()` 方法启动 Child Workflow 并返回其句柄。
如果您想在启动后执行某些操作，或者获取 Workflow/Run ID，或者能够在运行时执行 Signal，则这非常有用。

:::note

`ExecuteChildWorkflowAsync()` 是 `StartChildWorkflowAsync()` 和 `await handle.GetResultAsync()` 的辅助方法。

:::

```csharp
await Workflow.ExecuteChildWorkflowAsync((MyChildWorkflow wf) => wf.RunAsync());
```

## 设置父关闭策略 {#parent-close-policy}

[家长关闭政策](/parent-close-policy) 确定如果子 Workflow Execution 的父级更改为关闭状态（已完成、失败或超时），子级 Workflow Execution 会发生什么情况。

默认“父关闭策略”选项设置为终止子 Workflow Execution。

在 `ExecuteChildWorkflowAsync` 或 `StartChildWorkflowAsync` 的 [`ChildWorkflowOptions`](https://dotnet.temporal.io/api/temporalio.Workflows.ChildWorkflowOptions.html) 内部设置 `ParentClosePolicy` 属性，以指定当父 Workflow 关闭时 Child Workflow 的行为。

```csharp
await Workflow.ExecuteChildWorkflowAsync(
  (MyChildWorkflow wf) => wf.RunAsync(),
  new() { ParentClosePolicy = ParentClosePolicy.Abandon });
```
