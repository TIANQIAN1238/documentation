---
id: cancellation
title: "取消 Workflow - Go SDK"
sidebar_label: "消除"
description: Handle Cancellations in Temporal Workflows and Activities, set Activity Heartbeat Timeouts, and send Cancellation
  Handle Cancellations in Temporal Workflows and Activities, set Activity Heartbeat Timeouts, and send Cancellation
  requests from a Temporal Client in Go.
toc_max_heading_level: 4
keywords:
  - cancellation
  - go sdk
  - overview
tags:
  - Workflows
  - Go SDK
  - Temporal SDKs
---

该页面显示以下内容：

- 如何在 Workflow 内处理取消请求。
- 如何设置 Activity 心跳超时。
- 如何在 Activity 中侦听和处理取消请求。
- 如何从 Temporal CLIent 发送取消请求。
- 取消后的心跳。

<a id="handle-cancellation-in-workflow"></a>
## 在 Workflow 中处理取消 {#handle-cancellation-in-workflow}

可以编写 Workflow Definition 来处理 Go 的 `defer` 和
`Workflow.NewDisconnectedContext` API。在 Workflow Definition 中，有一个特殊的 Activity 负责处理清理工作
是否应取消执行。

如果 Workflow 收到取消请求，但所有活动都正常处理取消，和/或没有
活动将被跳过，然后 Workflow 状态将为“完成”。完全取决于业务的需求
流程和您的用例决定您是否要返回取消错误以显示已取消状态
或完成状态，无论取消是否已传播到和/或跳过活动。

<!--SNIPSTART go-features-cancellation-Workflow {"selectedLines": ["14-15", "18", "20-38", "41", "43-45", "47-50"]}-->
[示例应用程序/go/features/cancellation/Workflow.go](https://github.com/temporalio/documentation/blob/main/sample-apps/go/features/cancellation/workflow.go)
```go
// ...
// YourWorkflow is a Workflow Definition that shows how it can be canceled.
func YourWorkflow(ctx workflow.Context) error {
// ...
	activityOptions := workflow.ActivityOptions{
// ...
		HeartbeatTimeout:    5 * time.Second,
		// Set WaitForCancellation to true to have the Workflow wait to return
		// until all in progress Activities have completed, failed, or accepted the Cancellation.
		WaitForCancellation: true,
	}
	defer func() {
		// This logic ensures cleanup only happens if there is a Cancelation error
		if !errors.Is(ctx.Err(), workflow.ErrCanceled) {
			return
		}
		// For the Workflow to execute an Activity after it receives a Cancellation Request
		// It has to get a new disconnected context
		newCtx, _ := workflow.NewDisconnectedContext(ctx)
		// This Activity is only executed if
		err := workflow.ExecuteActivity(newCtx, a.CleanupActivity).Get(ctx, nil)
		if err != nil {
			logger.Error("CleanupActivity failed", "Error", err)
		}
	}()
// ...
	err := workflow.ExecuteActivity(ctx, a.ActivityToBeCanceled).Get(ctx, &result)
// ...
	// This call to execute the Activity is expected to return an error "canceled".
	// And the Activity Execution is skipped.
	err = workflow.ExecuteActivity(ctx, a.ActivityToBeSkipped).Get(ctx, nil)
// ...
	// Return any errors.
	// If a CanceledError is returned, the Workflow changes to a Canceled state.
	return err
}
```
<!--SNIPEND-->

<a id="handle-cancellation-in-an-activity"></a>
## 在 Activity 中处理取消 {#handle-cancellation-in-an-activity}

**如何在 Go 中处理 Activity 中的取消。**

确保 Activity 处于心跳状态以接收取消请求并停止执行。

<div className="copycode-notice-container">
  <a href="https://github.com/temporalio/documentation/blob/main/sample-apps/go/features/cancellation/activity.go">
查看源代码
  </a>{' '}
在应用程序代码的其余部分的上下文中。
</div>

```go
// ActivityToBeCanceled is the Activity that will respond to the Cancellation Request
func (a *Activities) ActivityToBeCanceled(ctx context.Context) (string, error) {
// ...
	// A for select statement is a common approach to listening for a Cancellation in an Activity
	for {
		select {
		case <-time.After(1 * time.Second):
			logger.Info("Heartbeating...")
			activity.RecordHeartbeat(ctx, "")
		// Listen for ctx.Done() to know if a Cancellation Request has propagated to the Activity.
		case <-ctx.Done():
			logger.Info("This Activity is canceled!")
			return "I am canceled by Done", nil
		}
	}
}
// ...
```

## 请求取消 {#request-cancellation}

**如何请求取消 Workflow 和 Go 中的活动。**

使用 `CancelWorkflow` API 通过其 ID 取消 Workflow Execution。

<div className="copycode-notice-container">
  <a href="https://github.com/temporalio/documentation/blob/main/sample-apps/go/features/cancellation/cancel/main.go">
查看源代码
  </a>{' '}
在应用程序代码的其余部分的上下文中。
</div>

```go
func main() {
// ...
	// Call the CancelWorkflow API to cancel a Workflow
	// In this call we are relying on the Workflow Id only.
	// But a Run Id can also be supplied to ensure the correct Workflow is Canceled.
	err = temporalClient.CancelWorkflow(context.Background(), cancellation.WorkflowId, "")
	if err != nil {
		log.Fatalln("Unable to cancel Workflow Execution", err)
	}
// ...
}
```

<a id="heartbeating-after-a-cancellation"></a>
## 取消后的心跳 {#heartbeating-after-a-cancellation}

有时您可能希望继续运行 Activity，即使在发出取消后也是如此。你可能想要
完全忽略取消并继续 Activity 执行，包括心跳，或者您可能想发送一个
取消后的最终心跳。即使当 Workflow 被取消时上下文被取消，您仍然可以
能够发送 Activity 心跳。

当您在取消后致电 `Activity.RecordHeartbeat` 时，
将记录 `WARN RecordActivityHeartbeat with error Error context canceled` 消息，并出现 `context canceled` 错误
will be returned from the call. However, the Heartbeat **has** still been sent.

## 重置 Workflow Execution {#reset}

重置 Workflow Execution 将终止当前的 Workflow Execution 并从
您在其 Event History 中指定的点。当 Workflow 由于非确定性错误或其他原因而被阻止时，请使用重置
阻止其完成的问题。

当您重置 Workflow 时，重置点之前的 Event History 将被复制到新的 Workflow Execution，并且
Workflow 从该点使用当前代码恢复。仅当您解决了根本问题（例如
删除不确定性代码。重置点之后取得的任何进度都将被丢弃。给出一个理由，当
重置，因为它将记录在 Event History 中。

<Tabs>

<TabItem value="web-ui" label="Web UI">

1. 导航至Workflow Execution详细信息页面，
2. 单击右上角下拉菜单中的 **重置** 按钮，
3. 选择要重置的事件 ID，
4. 提供重置原因，
5. 确认重置。

Web UI 显示可用的重置点，并在重置完成后创建指向新 Workflow Execution 的链接。

</TabItem>

<TabItem value="cli" label="Temporal CLI">

使用 `Temporal Workflow reset` 命令重置 Workflow Execution：

```bash
temporal workflow reset \
    --workflow-id <workflow-id> \
    --event-id <event-id> \
    --reason "Reason for reset"
```

例如：

```bash
temporal workflow reset \
    --workflow-id my-background-check \
    --event-id 4 \
    --reason "Fixed non-deterministic code"
```

默认情况下，该命令重置 `default` Namespace 中最新的 Workflow Execution。使用 `--run-id` 重置
具体运行。使用 `--Namespace` 指定不同的 Namespace：

```bash
temporal workflow reset \
    --workflow-id my-background-check \
    --event-id 4 \
    --reason "Fixed non-deterministic code" \
    --namespace my-namespace \
    --tls-cert-path /path/to/cert.pem \
    --tls-key-path /path/to/key.pem
```

重置后监视新的 Workflow Execution 以确保其成功完成。

</TabItem>

</Tabs>
