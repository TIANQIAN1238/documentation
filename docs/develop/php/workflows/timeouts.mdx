---
id: timeouts
title: "Workflow 超时 - PHP SDK"
sidebar_label: "超时"
description: "使用 Temporal PHP SDK 优化 Workflow Execution - 有效设置 Workflow 超时和重试策略。"
toc_max_heading_level: 4
keywords:
  - PHP
  - failure detection
  - timeouts
tags:
  - Activities
  - Workflows
  - Errors
  - Failures
  - PHP SDK
  - Temporal SDKs
---

<a id="workflow-timeouts"></a>
## Workflow 超时 {#workflow-timeouts}

每个 Workflow 超时控制 Workflow Execution 不同方面的最大持续时间。

在继续之前，我们要注意，我们通常不建议设置 Workflow 超时，因为 Workflow 被设计为长期运行且具有弹性。
相反，设置超时可能会限制其处理意外延迟或长时间运行的进程的能力。
如果您需要在特定时间段后在 Workflow 内执行操作，我们建议使用 Timer。

当 [启动 Workflow Execution](#workflow-timeouts) 时，设置 Workflow 超时。

- **[Workflow Execution 超时](/encyclopedia/detecting-workflow-failures#workflow-execution-timeout)** - 限制单个 Workflow Execution 可以执行的最长时间。
- **[Workflow 运行超时](/encyclopedia/detecting-workflow-failures#workflow-run-timeout)：** 限制单个 Workflow 运行可以持续的最长时间。
- **[Workflow 任务超时](/encyclopedia/detecting-workflow-failures#workflow-task-timeout)：** 限制 Worker 可以执行 Workflow 任务的最长时间。

在客户端代码中创建 `WorkflowOptions` 的实例并设置超时。

可用的超时有：

- `withWorkflowExecutionTimeout()`
- `withWorkflowRunTimeout()`
- `withWorkflowTaskTimeout()`

```php
$workflow = $this->workflowClient->newWorkflowStub(
    DynamicSleepWorkflowInterface::class,
    WorkflowOptions::new()
        ->withWorkflowId(DynamicSleepWorkflow::WORKFLOW_ID)
        ->withWorkflowIdReusePolicy(WorkflowIdReusePolicy::WORKFLOW_ID_REUSE_POLICY_ALLOW_DUPLICATE)
        // Set Workflow Timeout duration
        ->withWorkflowExecutionTimeout(CarbonInterval::minutes(2))
        // ->withWorkflowRunTimeout(CarbonInterval::minute(2))
        // ->withWorkflowTaskTimeout(CarbonInterval::minute(2))
);
```

<a id="workflow-retries"></a>
### Workflow 重试 {#workflow-retries}

Retry Policy 可以与超时配合，提供精细控制以优化执行体验。

如果发生故障，请使用 [Retry Policy](/encyclopedia/retry-policies) 重试 Workflow Execution。

默认情况下，Workflow Execution 不会重试，并且仅在某些情况下才应将重试策略与 Workflow Execution 一起使用。

可以使用 `RetryOptions` 对象的实例来配置 Retry Policy。
要启用 Workflow 的重试，您需要通过 Child Workflow 的 `ChildWorkflowOptions` 或顶级 Workflow 的 `WorkflowOptions` 提供 Retry Policy 对象。

```php
$workflow = $this->workflowClient->newWorkflowStub(
      CronWorkflowInterface::class,
      WorkflowOptions::new()->withRetryOptions(
        RetryOptions::new()->withInitialInterval(120)
      )
);
```
