---
id: run-worker-process
title: "Worker 流程 - Java SDK"
sidebar_label: "Worker 流程"
description: "本节介绍如何使用 Java SDK 实现 Worker"
toc_max_heading_level: 4
keywords:
  - Java SDK
tags:
  - Java SDK
  - Temporal SDKs
---

<a id="run-a-dev-worker"></a>
## 如何运行 Worker 进程 {#run-a-dev-worker}

[Worker 流程](/workers#worker-process) 是执行 Workflow 函数和 Activity 函数的地方。

- Worker 进程中的每个 [Worker 实体](/workers#worker-entity) 必须注册其可能执行的确切 Workflow 类型和 Activity 类型。
- 每个 Worker 实体还必须将其自身与一个 [Task Queue](/task-queue) 关联起来。
- 轮询相同 Task Queue 的每个 Worker 实体必须使用相同的 Workflow 类型和 Activity 类型进行注册。

[Worker 实体](/workers#worker-entity) 是 Worker 进程中侦听特定 Task Queue 的组件。

尽管多个 Worker 实体可以位于单个 Worker 进程中，但单个 Worker 实体 Worker 进程可能就足够了。
有关详细信息，请参阅 [Worker 调音指南](/develop/worker-performance)。

Worker 实体包含 Workflow Worker 和/或 Activity Worker，它们分别在 Workflow Execution 和 Activity 执行上取得进展。

在 [`WorkerFactory`](https://www.javadoc.io/doc/io.temporal/temporal-sdk/latest/io/temporal/worker/WorkerFactory.html) 实例上使用 `newWorker` 方法在 Java 中创建新的 Worker。

单个 Worker 实体可以包含许多 Worker 对象。
对 `WorkerFactory` 实例调用 `start()` 方法以启动在此过程中创建的所有 Worker。

```java
// ...
import io.temporal.client.WorkflowClient;
import io.temporal.serviceclient.WorkflowServiceStubs;
import io.temporal.worker.Worker;
import io.temporal.worker.WorkerFactory;

public class YourWorker {

  public static void main(String[] args) {

    WorkflowServiceStubs service = WorkflowServiceStubs.newLocalServiceStubs();
    WorkflowClient client = WorkflowClient.newInstance(service);
    WorkerFactory factory = WorkerFactory.newInstance(client);
    Worker yourWorker = factory.newWorker("your_task_queue");

    // Register Workflow
    // and/or register Activities

    factory.start();
  }
}
```

创建 Worker 实体后，注册 Worker 可以执行的所有 Workflow 类型和所有 Activity 类型。
Worker 可以仅使用 Workflow、仅使用活动或两者进行注册。

**操作指南：**

- [如何调整 Worker](/develop/worker-performance)

## 如何注册类型 {#register-types}

必须注册所有监听相同 Task Queue 名称的 Worker 才能处理完全相同的 Workflow 类型和 Activity 类型。

如果 Worker 轮询任务以查找它不知道的 Workflow 类型或 Activity 类型，则该任务将失败。
但是，任务失败不会导致关联的 Workflow Execution 失败。

使用 `Worker.registerWorkflowImplementationTypes` 注册 Workflow 类型，使用 `Worker.registerActivitiesImplementations` 注册 Activity 实现与 Worker。

对于 Workflow，Workflow 类型已注册为 Worker。
每个 Worker 实体只能注册一次 Workflow 类型。
如果您定义同一类型的多个 Workflow 实现，则会在注册时出现异常。

对于活动，Activity 实现实例向 Worker 注册，因为它们是无状态且线程安全的。
您可以在 Activity 实现构造函数中传递任意数量的依赖项，例如数据库连接、服务等。

以下示例显示如何将 Workflow 和 Activity 注册到 Worker。

```java
Worker worker = workerFactory.newWorker("your_task_queue");
...
// Register Workflow
worker.registerWorkflowImplementationTypes(GreetingWorkflowImpl.class);
// Register Activity
worker.registerActivitiesImplementations(new GreetingActivitiesImpl());
```

当您注册 Activity 的单个实例时，您可以让多个 Workflow Execution 实例调用同一个 Activity。
Activity 代码必须是线程安全的，因为调用它的每个 Workflow Execution 都会运行 Activity 代码的同一实例。

对于 `DynamicWorkflow`，只能向 Worker 注册一个扩展 `DynamicWorkflow` 的 Workflow 实现。
以下示例演示如何使用 Worker 注册 `DynamicWorkflow` 和 `DynamicActivity` 实现。

```java
  public static void main(String[] arg) {

    WorkflowServiceStubs service = WorkflowServiceStubs.newInstance();
    WorkflowClient client = WorkflowClient.newInstance(service);
    WorkerFactory factory = WorkerFactory.newInstance(client);
    Worker worker = factory.newWorker(TASK_QUEUE);

    /* Register the Dynamic Workflow implementation with the Worker. Workflow implementations
    ** must be known to the Worker at runtime to dispatch Workflow Tasks.
    */
    worker.registerWorkflowImplementationTypes(DynamicGreetingWorkflowImpl.class);

    // Start all the Workers that are in this process.
    factory.start();

    /* Create the Workflow stub. Note that the Workflow Type is not explicitly registered with the Worker. */
    WorkflowOptions workflowOptions =
        WorkflowOptions.newBuilder().setTaskQueue(TASK_QUEUE).setWorkflowId(WORKFLOW_ID).build();
    WorkflowStub workflow = client.newUntypedWorkflowStub("DynamicWF", workflowOptions);
    /**
     * Register Dynamic Activity implementation with the Worker. Since Activities are stateless
     * and thread-safe, we need to register a shared instance.
    */
    worker.registerActivitiesImplementations(new DynamicGreetingActivityImpl());

    /* Start Workflow Execution and immmediately send Signal. Pass in the Workflow args and Signal args. */
    workflow.signalWithStart("greetingSignal", new Object[] {"John"}, new Object[] {"Hello"});

    // Wait for the Workflow to finish getting the results.
    String result = workflow.getResult(String.class);

    System.out.println(result);

    System.exit(0);
  }
}
```

您可以注册多个特定于类型的 Workflow 实现以及单个 `DynamicWorkflow` 实现。
您只能注册一个使用 Worker 实现 `DynamicActivity` 的 Activity 实例。
